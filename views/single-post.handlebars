{{> post-info post}}

{{#if loggedIn}}
<form class="comment-form">
  <div>
    <textarea class="comment-body" name="comment-body" placeholder="Comment"></textarea>
    <button class="comment-submit" type="submit">Comment as {{username}}</button>
  </div>
  <div>
    
  </div>
</form>
{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
{{/if}}